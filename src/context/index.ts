/* eslint-disable @typescript-eslint/no-explicit-any */
import { createContext } from "react";

export const CalendarContext = createContext({} as any);
export const ToastErrorContext = createContext({} as any);
